TODO: 
	Delete temp folder
	Generator shall make duplicates
	var sd = new SortedDictionary<string, int>(); — does not allow duplicates

На входе есть большой текстовый файл, где каждая строка имеет вид Number. String
Например:
415. Apple
30432. Something something something
1. Apple
32. Cherry is the best
2. Banana is yellow

Обе части могут в пределах файла повторяться. Необходимо получить на выходе другой файл, где все строки отсортированы. Критерий сортировки: сначала сравнивается часть String, если она совпадает, тогда Number.

Т.е. в примере выше должно получиться
1. Apple
415. Apple
2. Banana is yellow
32. Cherry is the best
30432. Something something something

Требуется написать две программы:
1. Утилита для создания тестового файла заданного размера. Результатом работы должен быть текстовый файл описанного выше вида. Должно быть какое-то количество строк с одинаковой частью String.
2. Собственно сортировщик. Важный момент, файл может быть очень большой. Для тестирования будет использоваться размер ~100Gb.
При оценке выполненного задания мы будем в первую очередь смотреть на результат (корректность генерации/сортировки и время работы), во вторую на то, как кандидат пишет код. Язык программирования на усмотрение кандидата любой из C#/C++/Delphi. С нашей точки зрения выбранный язык ни на что не влияет.






===============================
Ответы на типичные вопросы:
> Какой диапазон значений может принимать часть Number?
32-битное беззнаковое целое
> Часть String состоит только из букв и пробелов (цифры, специальные символы отсутствуют)? 
Английский текст, пробелы, цифры и знаки препинания.
> Комбинации букв должны обязательно представлять собой слова английского языка?
Нет.
> Длина (размер) части String одной строки имеет ограничение?
Не больше 1024 символов.
> Размер генерируемого файла - это переменная, которую можно задать?
Да, генератор должен уметь создавать файлы заданного размера. 
> Как происходит генерация текстовой части?
И цифры, и текст случайно. 
> Есть ограничения по времени работы и ресурсам?
По ресурсам. Программа будет выполняться на Windows x64 на машине с 16Gb памяти. Свободного места на диске много.
По времени работы. Как такового ограничения нет, но будет сравниваться с решениями других кандидатов. Фотофиниш мы проводить не будем, но разницу в разы наверно заметим.
>Можно ли использовать сторонний код, готовые библиотеки и т.д.?
Можно.
> Будет ли производиться проверка только на равномерно распределенных случайных значениях, или также будет производиться испытание на (частично) отсортированных значениях?
Равномерно распределённых. В качестве бонуса кандидат может написать, что бы он поменял, если бы речь шла о частично/почти полностью отсортированных значениях.

1 min 9 sec
32 sec